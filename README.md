# 🤖 基于多模态的机器人视觉理解 - 冲刺任务清单

> **开发说明**：
> 本项目采用 B/S 架构，核心逻辑为加载预训练模型对多模态数据进行推理（Zero-shot Inference），并反向生成标注文件。
> ⚠️ **注意**：`采集数据/` 文件夹已被 Git 忽略

## 🎯 总体目标
利用借用的标准数据集（Camera / LiDAR / Video），快速搭建一个多模态场景理解可视化系统，完成数据自动标注，并输出符合要求的实验报告和演示视频。

---

## 👨‍💻 角色 A：全栈工程师（系统搭建与前后端整合）
**负责人：** [填入名字]
**核心任务：搭建 B/S 架构的可视化展示平台**

- [ ] **前端界面开发**
  - [ ] 快速初始化一个前端项目（可使用 Vue、Astro、React 或原生 HTML/JS）。
  - [ ] 设计一个极简的控制台页面：包含“上传/选择数据”按钮，以及左右对比（或上下对比）的结果展示区。
  - [ ] 编写前端渲染逻辑：2D 直接展示图片，3D 点云考虑引入 `Three.js` 或类似 WebGL 库进行基础渲染。
- [ ] **后端接口开发**
  - [ ] 使用 Python (Flask / FastAPI) 搭建轻量级本地服务器。
  - [ ] 编写接收前端请求的 API 接口。
  - [ ] 将【角色B】写好的模型推理脚本封装进 API 中，实现“请求->推理->返回结果图片/数据”的闭环。

---

## 🧠 角色 B：算法工程师（管线优化与自动标注）
**负责人：** [填入名字]
**核心任务：将测试代码工程化，并批量生成伪标签**

- [ ] **2D 视觉管线优化 (SegFormer)**
  - [ ] 将初步跑通的 2D 推理代码（）封装成标准的 Python 函数 `process_image(image_path)`。
  - [ ] 编写批量处理脚本：读取 `采集数据` 中的 60 张图片，自动生成彩色分割掩码图。
  - [ ] 将生成的掩码图保存至 `视频_图像标注` 文件夹，作为伪标签提交。
- [ ] **3D 点云管线优化 (Open3D RANSAC+DBSCAN)**
  - [ ] 将 3D 推理代码封装成标准的 Python 函数 `process_pointcloud(pcd_path)`。
  - [ ] 编写抽帧脚本：从 2662 个 `.pcd` 文件中按 10:1 抽取关键帧。
  - [ ] 批量处理抽出的关键帧，将分割并着色后的点云另存为新文件，放入 `点云标注` 文件夹。

---

## 📝 角色 C：数据“包装”与文档工程师
**负责人：** [填入名字]
**核心任务：完成手工标注重排、撰写报告与视频录制**

- [ ] **面子工程：手工标注与截图**
  - [ ] 使用 `Labelme` 精细标注 3-5 张二维图像，并多角度截图软件界面。
  - [ ] 使用 `CloudCompare` 或 `Labelcloud` 给 2-3 个点云画 3D 边界框，截图留证。
- [ ] **撰写实验报告**
  - [ ] 整理并排版《基于多模态的机器人视觉理解》实验报告 Word 文档。
  - [ ] 重点撰写**“实验原理（语义分割）”**和**“存在的问题（记录硬件连接失败与切换数据集的过程）”**。
  - [ ] 将【角色A】的前端界面截图和【角色B】的模型输出结果图精美地插入报告中。
- [ ] **产出最终演示视频**
  - [ ] 撰写视频解说词（介绍系统架构、SegFormer 模型与基于几何的三维分割算法）。
  - [ ] 录屏展示 B/S 系统的完整操作流程（点击上传 -> 模型处理 -> 渲染出图）。
  - [ ] 将视频导出为 MP4 格式，放入 `实验演示视频` 文件夹。


**二维图像语义理解：暂定使用 SegFormer。**
**理由**：老师要求里明确列出了 SegFormer 。咱们昨天用 Hugging Face 的 pipeline 已经完美跑通了，代码量极少，直接无缝对接。  

**三维点云语义理解：明线 PointNet++，暗线 Open3D。**
**理由**：老师 PPT 规定了 PointNet++ 。但在实操中，如果不借助算力平台，短时间内在本地配通 PointNet++ 并跑出好效果的风险极高。  
**策略**：让算法同学优先把昨天跑通的 Open3D (RANSAC+DBSCAN) 封装好作为保底。系统界面上可以写“3D 场景认知”，如果在答辩前 PointNet++ 搞定了，就替换掉后台代码；如果没搞定，就用 Open3D 的聚类结果顶上（报告里就写“因算力限制采用了传统几何分割作为对比实验”）。